# Purpose: Frontend environment variable template (no secrets).
# Interactions: Copy to apps/frontend/.env.local. NEXT_PUBLIC_API_URL is read by
# src/lib/api.ts to call the FastAPI backend at localhost:8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
